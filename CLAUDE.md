# autoworker-story

썰/스토리 야담 유튜브 **내러티브** 자동화 파이프라인.
정보성 채널(마스코트 1개, `autoworker-youtube`)과 달리 **주연·조연 캐릭터 + 배경 asset**이 등장한다.
`autoworker-youtube`는 **코드 참조만** 하고(fork 아님), 검증된 조각만 이식했다.

## 핵심 원칙 (빈밥상 실증으로 검증됨)

캐릭터 일관성 3종 세트:
1. **턴어라운드 ref** — 인물당 1장(정면·3/4·측면·후면, 흰 배경, 텍스트 0).
2. **trait-lock 텍스트** — `physical{age,build,hair,eyes,uniqueFeatures}` + `negatives`. 특히 **`build`(키/체형/연령)가 필수** (없으면 다 성인으로 드리프트). hex 색은 약하니 말로.
3. **씬별 cast** — 씬마다 등장 인물 id만 골라 그 턴어라운드 ref만 첨부(전부 X, 최대 5장).

- **인물마다 식별 앵커 1개 필수** (`anchorProp` + lock 문장에 포함) — 실루엣/착용 소품/시그니처 색/큰 머리·얼굴 특징 중 하나, 캐스트 내 축·색 중복 금지. 동물·신령 등 비인간 인물은 얼굴 잠금이 약하므로 몸에 두른 착용 소품(발목에 맨 색 끈, 목에 건 방울 등)이 얼굴보다 강한 앵커.
- **★얼굴은 인물마다 다르게 명시한다 (2026-08-16 사용자 지적 · ★2026-09-01 화풍 교체로 6축 중 셋은 폐기 — 위 「화풍」 절의 얼굴 규칙을 먼저 읽을 것)** — lock에 `age`·`build`·앵커만 쓰면 **얼굴이 전부 비슷해진다.** 생성기가 기본 미남미녀 얼굴로 수렴하기 때문. 인물마다 **얼굴 지문(faceprint)**을 lock에 박는다: ①얼굴형(각진 턱/둥근 볼/갸름한 역삼각/넓은 광대/긴 말상) ②눈매(홑꺼풀 가는 눈/쌍꺼풀 큰 눈/처진 눈/치켜뜬 눈) ③눈썹(굵고 짙은/가늘고 처진/일자) ④코(뭉툭한/오뚝한/낮고 넓은) ⑤입(얇은/도톰한/큰) ⑥표식(점·주근깨·흉터·보조개·팔자주름) 중 **최소 4개를 서로 다르게 조합**한다. **특히 편이 달라도 같은 연령대·성별이 겹치면 반드시 갈라 준다** (02편의 억쇠 25·삼돌 28·두식 23·만복 30·개똥 28은 전부 젊은 남자라 얼굴형·눈매를 전부 다르게 줬다).
- **★lock의 포지티브 문장이 style.json 네거티브를 이긴다 (2026-08-17 실측)** — style.json에 `NOT scary`·`no horror or zombie styling`·`no photographic skin texture`가 있어도, lock에 `skeletal`·`hollow eye sockets`·`skin stretched over the bones`처럼 **포지티브로 반대를 주문하면 그쪽이 이긴다.** 실제로 개똥 아비가 해골처럼, 곽 서방이 반사실적 주름 질감으로 나왔다. 두 가지를 지킨다:
  ① **질감·병색·노화는 한 번만, 약하게.** 주름을 lock에서 세 번 강조하면(‘deeply wrinkled’+‘lost in wrinkles’+‘deep nasolabial folds’) 화풍이 셀 채색을 벗어난다.
    - **★넷을 넘으면 화풍만이 아니라 뷰 크롭까지 흔든다** (2026-09-03 13편 실측). 진메댁(52여)에 주름 신호 넷(`crow's-foot lines`+`sun-mottling`+`a jaw that has begun to slacken`+`soft lines at its corners`)을 넣었더니, **혼자만 선이 굵고 채색이 평평한 만화체**로 나오고 **4뷰 중 셋이 허리 위가 아니라 흉상으로 잘렸다.** 넷을 하나로 줄이니 화풍과 크롭이 동시에 돌아왔다. 해봉(66남)도 같았다 — 수척 신호 넷(`very thin`+`hollow cheeks`+`hollow collarbones`+`deeply sun-weathered`)에 **여든 넘은 병자**로 나왔고, preset에 `never haggard`가 있는데도 lock이 이겼다. **노화·수척은 축 하나당 신호 하나가 상한이다.**
  ② **하반신을 요구하는 문장을 lock에 쓰지 않는다.** `다리를 절어 몸이 한쪽으로 기운다` 같은 서술은 `framing: waist`를 무시하고 전신을 그리게 만든다. **자세·장애·동작은 lock이 아니라 씬 `visual_desc`에서 연출한다** — lock은 ‘이 사람이 누구인가’만 고정하는 자리다.
  - **★표정도 마찬가지다 — lock에 표정을 적으면 편 전체에서 그 얼굴만 나온다** (2026-08-28 08편 실측, 사용자 지적). 명선 lock의 `a single vertical frown line between the brows`(미간 세로 주름)와 `sharply angled brows` 때문에 **이 편의 감정 정점(두 해 만에 이름을 부르는 씬)이 화난 얼굴로 소리치는 그림**이 됐다. 끝순은 `small narrow smiling eyes` 때문에 절이 헐리는 대목까지 웃고 있었다. STYLE 블록에 `FACES MUST ACT: 표정은 그 순간을 따른다`가 있어도 **lock이 이긴다.**
    - **가르는 기준: 영구적 신체 특징이냐, 그 순간의 표정이냐.** `눈가에 깊게 팬 주름`은 특징이라 써도 되고 `웃는 눈`은 표정이라 안 된다. `미간 주름`·`꺾인 눈썹`·`굳은 턱`·`지친 얼굴`은 전부 표정 쪽이다.
    - 기질(성마름·입담 등)은 **얼굴형·눈매 같은 중립 축**으로 표현한다 — 명선은 마름모 얼굴+치켜뜬 가는 눈으로 충분히 날카롭고, 눈썹은 `THIN HIGH-SET BROWS WITH A GENTLE ARCH`로 중립을 준다.
    - 시트에서만 중립을 강제하고 싶으면 `negatives_turnaround`에 `NEUTRAL RESTING FACE on this sheet — no scowl, no glare, and no smile either`를 넣는다. **씬 `negatives`에는 절대 넣지 않는다**(사건을 못 그린다).
  ③ **★흐린 색을 lock에 쓰면 채널 화풍이 통째로 죽는다 (2026-08-21 07편 실측).** style.json preset이 `BRIGHT VIVID SATURATED`·`BOLD CLEAN LINEART`를 요구해도, lock에 `FADED GREY`·`PALE GREY`·`PALE OCHRE`처럼 **채도를 낮추는 색을 적으면 그림 전체가 탁한 스케치풍으로 넘어간다** — 라인아트가 흐려지고 눈에서 홍채·캐치라이트가 사라진다. 같은 배치에서 `PERSIMMON RED-BROWN`(순임)만 정상으로 나왔고, `FADED GREY`(덕배)·`PALE GREY`(귀덕네)가 무너졌다. 색을 바꿔 재생성하니 둘 다 살아났다. **인물 색은 반드시 채도 있는 염색색으로 적고(`richly dyed DEEP INDIGO BLUE`), 구분은 명도가 아니라 색상으로 낸다.** 가난·낡음은 색이 아니라 `patched`·`worn`·`sleeves pushed up` 같은 상태어로 표현한다.
  ④ **★앵커가 하반신에 있으면 `framing: waist`와 싸운다** (2026-08-21 실측). 표 노인의 앵커를 '정강이에 감은 감발'로 잡았더니 모델이 그것을 보여 주려고 **전신을 그려 얼굴이 작아졌다.** waist 인물의 앵커는 **가슴·어깨·목·허리 위**에서 고른다(가슴을 가로지른 전대, 어깨에 멘 자, 목에 건 물표). 하반신 앵커가 꼭 필요하면 `framing: full`을 함께 지정한다.
  ⑤ **★체형을 줄이는 말을 쓰면 비율이 아이가 된다 — 대두** (2026-09-03 13편 엄 첨지 실측, 사용자 지적 *"사람 비율이 다른거랑 안맞아 대두야"*). `Short and heavy-set` + **`short limbs`** + `a thick neck` + `his head sits forward of his shoulders`를 겹쳤더니 **3~4등신에 팔이 겨드랑이 바로 아래에서 나오는** 캐리커처가 됐다. 같은 배치의 나머지 여덟은 정상이었다. 원인은 위 §「아이 신호를 포지티브로」의 뒷면이다 — **`large head for the body`가 아이 신호이므로, 몸을 줄이는 말은 전부 그쪽으로 끌린다.** **작은 키·굽은 등·비만은 성인 비율을 함께 박아야 한다**: `FULL ADULT BODY PROPORTIONS`·`a SMALL HEAD on BROAD SQUARE SHOULDERS`·`arms of normal adult length hanging well below the waist`. `short limbs`·`thick neck`·`head sits forward of the shoulders`는 쓰지 않는다(굽은 등은 `the upper back slightly rounded` 정도로만).
- **★lock·negatives는 짧게 — 길면 화풍·구도 지시를 밀어낸다** (2026-08-17 03편 실측). 얼굴 6축을 완전한 문장으로 풀고 negatives를 8~9줄 달았더니 프롬프트가 5,700자가 되고 **채널 화풍 지시가 55% 지점으로 밀려**, `do NOT repeat the same angle twice`(→4뷰 각도 중복)와 `ONE consistent drawing style`(→인물마다 화풍 갈림)이 함께 묻혔다. 얼굴은 **명사구 나열**로, negatives는 **그 인물만의 금지 3~4줄**로(공통 금지는 style.json에 이미 있다). 점검: 생성된 `*.prompt.txt`에서 `Modern Korean webtoon`이 앞 35% 안에 나와야 한다. 상세는 SKILL.md 레지스트리 절.
- **★인물 `negatives`는 씬 프롬프트에도 그대로 들어간다** (2026-08-18 04편 실측) — 턴어라운드에만 걸 조건을 여기 적으면 씬에서 사건을 못 그린다(소에 '쟁기 금지'를 걸어 봄갈이를 막고, 노파에 '지팡이 금지'를 걸어 대본 동작과 충돌했다). 턴어라운드 전용 금지는 `negatives_turnaround`로 분리하고, `negatives`에는 화풍·복식·연령·문화 오염 방지만 남긴다. **자세·소품·동작 금지는 lock도 negatives도 아니라 씬 `visual_desc`의 몫이다.**
  - **★`negatives_turnaround`는 2026-08-28에야 실제로 구현됐다** (그전까지 이 문단은 규칙만 있고 코드가 없었다 — 두 스크립트가 `negatives`만 읽었다). 지금은 `turnaround.py`의 `_negs()`가 시트에서만 `negatives + negatives_turnaround`를 합쳐 쓰고, `build.py`는 `negatives`만 읽는다. **얼굴형·눈매·눈썹 금지와 표정 금지(`no tears`·`no angry expression`)는 전부 `negatives_turnaround`로** — 표정을 씬 프롬프트에 걸면 대본이 요구하는 사건을 못 그린다. 08편 실측: 성별·연령 드리프트를 고치려고 금지문을 늘렸더니 씬 프롬프트가 10,967자가 됐고, 갈라 놓으니 내려왔다.
  - **★성별·연령 드리프트는 negatives로 못 막는다 — 얼굴 축을 바꿔야 한다** (2026-08-28 08편 실측). 스물둘 남자에게 `NOT a woman`을 세 줄 붙여도 계속 여자로 나왔고, **큰 쌍꺼풀 눈·가는 눈썹·얇은 입술**이라는 얼굴 축을 **넓은 간격의 가는 아몬드 홑꺼풀 눈·낮게 깔린 좁은 직선 눈썹·각진 턱끝·후두융기**로 바꾸고 나서야 남자가 됐다. 열한 살 아이도 같다 — `build`에 '또래보다 크다'만 쓰면 청년이 된다. `a CHILD`, `childlike proportions with a large head for the body`처럼 **아이 신호를 포지티브로** 박는다.
  - **★중·노년 여성의 나이는 남성과 달리 주름과 화풍이 맞바꿈이다** (2026-09-03 13편 실측). 같은 배치에서 **남성 노인은 나이가 그냥 버텼다**(석주 70·해봉 66·엄 첨지 58 전부 한 번에 나왔다). 그런데 여성은 12편 순금(37)이 **20대 초반**으로, 13편 진메댁(52)이 **20대**로 나왔다. preset의 `women are pretty with a softer jaw`가 여성에게만 더 세게 걸리기 때문으로 보인다. 주름을 늘리면 나이는 맞는데 위 ①대로 화풍이 갈리고, 줄이면 화풍은 맞는데 서른으로 밀린다. **실측으로 찾은 절충값은 「주름 2개(눈가·팔자) + 흰머리 2군데 + 처진 턱선 + 나이를 부정형으로 한 번」**이다 (`her face reads clearly as a woman PAST FIFTY, never as a woman in her thirties`). 윤례(42)는 이 배합으로 한 번에 나왔다.
  - **★★남성도 같은 맞바꿈이 있다 — 가르는 것은 「신호 개수」가 아니라 「강조어」다** (2026-09-03 14편 하 객주 58남, 여섯 번 뽑아 확인). 13편은 *"남성 노인은 나이가 그냥 버텼다"*고 적었는데, **살집 있고 각진 얼굴**은 안 버텼다. 같은 배치의 노달수(57남 마른 긴 얼굴)·늙은 서기(72남)는 한 번에 나왔다.
    - 1차 — 체형 2(thick shoulders + heavy belly) + 얼굴 2(broad square + heavy wide jaw) + 노화 2 = **6개** → **흉상으로 잘리고 화풍이 사실적으로 갈렸다.** 같은 배치의 감내댁(67여)도 노화 신호 6개로 **굵은 선 만화체**가 됐다.
    - 2차 — 축 하나당 하나로 줄이고 `FULL ADULT BODY PROPORTIONS`를 박으니 **화풍·크롭이 동시에 돌아왔다.** 감내댁은 이 한 번으로 완전히 해결됐다(허리 위 + 화풍 일치).
    - 3~4차 — 그런데 하 객주만 **마흔 살로 보였다**(대본은 쉰여덟, 26회 등장). 신호를 강하게 되돌리니(`DEEP NASOLABIAL FOLDS`·`GOING CLEARLY GREY`) 나이는 맞았지만 **화풍이 또 갈렸다.**
    - **★해답은 개수가 아니라 강조어였다.** 신호 개수(팔자주름 1 + 관자놀이 흰머리 1)는 그대로 두고 **`DEEP`·`CLEARLY` 같은 강조 부사만 떼니** 나이와 화풍이 같이 맞았다. 대문자 강조는 그 자체가 신호 강도로 읽힌다.
    - **나이 부정형 한 줄(`reads as a man PAST FIFTY-FIVE, never as a man in his thirties`)은 화풍을 안 깨면서 나이를 잡는 가장 값싼 수단**이다 — 13편이 여성에게 쓴 것을 남성에도 그대로 쓰면 된다.
  - **★배경 시트에 사람이 그려지면 지운다** (2026-09-03 14편 누룩방 실측). `desc`에 인물을 안 썼는데도 마당에 한복 입은 여자 넷이 들어왔다. 시트는 씬마다 ref로 붙으므로 그 군중이 딸려 들어온다. `desc`에 **`THE PLACE IS COMPLETELY EMPTY OF PEOPLE — no figures, no villagers, nobody in the yard or the doorway. This is a set, not a scene.`** 를 박고 다시 뽑는다.
  - **★시트가 이야기의 반전을 미리 그려 버릴 수 있다** (같은 실측, 14편 빈집). `desc`에 안 썼는데 **굴뚝에 연기**가 그려졌다 — 그 편에서 연기는 ②비트에야 처음 오르는 장치다. 시트를 고치는 대신 **반전 이전 씬의 `visual_desc`에 `no smoke from the chimney, the chimney is cold`를 적어** 막는다(시트는 장소를 정의할 뿐이고 씬이 이긴다).
  - **★흰머리는 가려지지 않는 자리에 둔다** (같은 실측). 진메댁의 흰머리를 `GREY AT THE PARTING`(가르마)에 뒀더니 **머릿수건에 덮여 통째로 사라졌다.** `GREY IN WIDE STREAKS AT BOTH TEMPLES`(관자놀이)로 옮기니 수건 앞으로 나와 살았다. **머릿수건·갓·망건을 쓴 인물은 연령 신호를 그 아래에 두지 말 것.**
- **★★`visual_desc`에 인물이 나오면 반드시 `cast`에도 넣는다 — 안 넣으면 그 씬만 얼굴이 딴사람이 된다** (2026-09-02 11편 1편 씬07·09 실측, 사용자 지적 *"7번은 얼굴이 달라진거같다"*). `cast`가 비면 build.py가 **군중 씬으로 판단**해 세 가지를 한꺼번에 뺀다: ①턴어라운드 ref 미첨부(얼굴을 잡아 줄 근거가 사라진다) ②`scene_preset` 대신 **`scene_preset_nocast`** — 여기엔 미형 조항도 홍채 색 지정도 없다 ③`scene_negative_nocast`의 **`NO LARGE FACE ANYWHERE`** 가 걸려 얼굴을 크게 그리지 말라는 지시까지 들어간다. `{id}` 토큰은 안전망이 lock만 조용히 치환해 주므로 **프롬프트만 보면 멀쩡해 보이고 에러도 안 난다.**
  - `cast`를 비우는 것은 **인물이 정말 한 명도 안 나오거나(빈 방·소품 클로즈업), 이름 없는 군중만 멀리 보이는 씬**뿐이다.
  - 2026-09-02부터 `build.py`가 이 경우 **경고를 찍는다**(`visual_desc 에 … 가 있는데 cast 에 없습니다`). 생성 로그에 이 줄이 보이면 무시하지 말 것.
  - 전수 점검: `visual_desc`의 `{id}` 토큰 집합이 `cast`의 base id 집합에 포함되는지 본다.
- **★홍채는 「거의 검정에 가까운 짙은 갈색」이다** (2026-09-02 사용자 지적). 처음엔 색 이름을 아예 안 써서 **회색 눈**이 나왔고, `WARM DARK-BROWN`으로 고쳤더니 이번엔 **호박빛으로 너무 밝았다.** 확정 문안은 `a big VERY DARK BROWN iris, almost BLACK — 빛이 닿는 곳만 아주 살짝 따뜻하고, 테두리는 더 짙게, 작은 캐치라이트 2~3개; 눈은 DARK 하게 읽혀야 하고 결코 amber·light brown·grey가 아니다`. 네거티브에도 `밝은 갈색·호박색·헤이즐 금지`를 함께 건다. **눈동자 색은 시트(클로즈업)에서만 티가 나고 씬에서는 잘 안 보이므로, 반드시 턴어라운드 원본으로 확인한다.**
- **★★씬 `visual_desc`에는 「구도」와 「표정」을 반드시 쓴다 — 안 쓰면 눈이 뿌옇고 얼굴이 무표정해진다** (2026-09-02 12편 157씬 전수 검수, 사용자 지적 *"눈동자가 왜 뿌옇게 나오지"* · *"표정이 다들 생동감이 없어"*). 12편은 **구도 지시 없는 씬 92% · 표정 지시 없는 씬 87%**였다. 둘 다 `style.json`으로는 못 고친다.
  - **구도** — 프리셋에 `★COMPOSITION: 인물은 화면에서 크게`가 있어도, 씬이 마당 전경을 요구하면 진다. 얼굴이 화면 높이의 1/10이 되면 **홍채를 그릴 화소가 없어 눈이 회색 얼룩**이 된다. 프리셋의 `VERY DARK BROWN iris`는 그릴 자리가 있을 때만 지켜진다. 같은 편에서 `Waist-up, she is the clear centre.`가 적힌 씬만 눈이 제대로 나왔다.
  - **표정** — lock에 쓰면 편 전체가 그 얼굴로 굳으므로(위 08편 실측) **씬 `visual_desc`가 유일한 자리**다. 그런데 아무도 안 쓰면 모델은 기본값인 무표정 미인 얼굴로 간다. 프리셋의 `FACES MUST ACT: 표정은 그 순간을 따른다`는 **그 순간에 표정이 적혀 있을 때만** 작동한다. 대본의 그 대목을 읽고 씬마다 다르게 쓴다(전부 같은 표정이 되면 그것도 실패다).
  - **점검** — `check_desc.py {편 폴더...}` 를 **그림 뽑기 전에** 돌린다.
  - **★2026-09-02부터 `build.py`가 이것을 게이트로 막는다** (사용자 지시 *"표정지시는 항상 모든 씬에 들어가야"*). 인물이 나오는 씬에 구도나 표정이 없으면 **한 장도 생성하지 않고 종료코드 2로 멈춘다.** 경고만으로는 안 막혔다 — 12편은 표정 없는 얼굴 143장을 다 뽑고 나서야 사람이 알아챘고, 유료 엔진이라 그대로 돈이었다. 강행은 `--allow-undirected`.
- **★★이름 있는 인물을 「작게·흐리게」 그리라고 쓰면 ref가 죽는다** (2026-09-02 12편 1편 씬49 실측, 사용자 지적 *"49씬은 왜 다른사람 얼굴이야"*). 씬 서술에 `He is smaller, beyond the wall`이라고 썼더니 종배의 얼굴이 작아졌고, **홍채와 이목구비를 그릴 화소가 없어지자 모델이 턴어라운드 시트를 버리고 평균적인 젊은 얼굴을 만들어 넣었다.** ref는 정상적으로 붙어 있었다 — **크기가 곧 ref의 생존 조건**이다.
  - 멀리 두어야 하는 장면은 **어깨 너머 구도**로 잡는다. 보는 사람을 앞쪽에 뒷모습·옆얼굴로 두고, 보이는 대상을 화면 가운데 허리 위로 크게. 씬49를 그렇게 고쳐 해결했다.
  - `build.py`가 시트를 붙인 씬마다 프롬프트에 **`EVERY character who has a reference sheet must be drawn LARGE ENOUGH THAT THEIR FACE IS CLEARLY READABLE … Only unnamed background people may be small and softly out of focus`**를 항상 건다.
  - 게이트도 이것을 **`인물축소`**로 잡는다. 판정은 낱말이 아니라 **어순** — 흐림 낱말 바로 앞의 표지가 대명사·`{id}`면 배역(지적), 풍경 낱말이면 배경(정상), 관사+명사면 이름 없는 단역(정상).
- **★표정은 있기만 하면 안 되고 씬마다 달라야 한다** (사용자 지적 *"표정이 좀 다들 생동감이 없어"*). `check_desc.py`가 같은 표정 구절이 3개 씬 이상에 반복되면 지적한다. 얼굴 부위 앞에 소유격(his/her/their)이 있을 때만 표정으로 센다 — 안 그러면 `fire mouth glowing orange` 같은 사물 표현을 표정으로 센다.
- **★해부·군중·글자는 씬 서술이 아니라 채널 프리셋에서 막는다** (2026-09-02 12편 실측, `style.prev_20260902.json`).
  - **해부** — 종전 프리셋에 해부 문장이 한 줄도 없어서 **몸에 안 붙은 팔**(3편 씬35), **화면을 가로지르는 거대한 손 두 개**(씬38), **시루 위로 넘어온 셋째 손**(씬42), **무릎 아래까지 내려간 팔**(1편 씬12)이 나왔다. `★ANATOMY IS CORRECT: 손을 세어 보라 — 주인 없는 손도, 몸 없이 프레임 밖에서 들어오는 팔도 없다`를 프리셋에, 대응 금지문을 네거티브에 넣었다.
  - **군중** — 「똑같이 예쁜 젊은 여자 한 줄」 금지문이 **씬 서술에 손으로 붙여 둔 것**이라 붙인 씬에만 걸렸고, 안 붙은 3편 씬01·42가 정확히 그 모양으로 나왔다. **채널 프리셋으로 올렸다** — 이런 종류는 전 씬에 걸려야 한다.
  - **글자** — `WRITTEN THINGS CARRY NO LEGIBLE MARKS`가 있는데도 대본이 문서·간판·장부를 요구하면 글자가 그려졌다(3편 씬13 간판 한글 · 씬47 종이에 또렷한 글자 · 씬04·43 펼친 책 · 1편 씬10 벽). `이 지시가 씬 서술을 이긴다(overrides the scene description)`를 명시하고, 네거티브에도 물건 이름을 같이 적었다.
- **★씬 검수는 「기계 → 격자 → 원본」 3단이다** (2026-09-02 11편 1편 57씬 전수 검수에서 확립).
  ① `check_scenes.py {P}` — 흰 액자·레터박스·이중 패널·안쪽 여백. 해상도·용량으로는 절대 안 걸리는 종류다.
  ② **라벨 붙인 12장 격자**를 만들어 훑는다(씬 번호를 그려 넣어야 어느 컷이 문제인지 바로 짚힌다). 화풍 편차·얼굴 드리프트·인물 중복은 이 단계에서 보인다.
  ③ 의심 가는 컷만 **원본**으로 연다.
  ★이 방식으로 57씬 중 5장(32·40·45·47·54)을 잡았다. **전부 「대본의 핵심 소품이 안 그려진」 유형**이었다 — 국자·자물쇠·짚신·찬장이 사라지고 인물만 서 있었다. 화풍·얼굴 문제가 아니라 **연출 지시가 약해서 생긴 문제**이므로, 고칠 때는 화풍을 건드리지 말고 **`visual_desc`를 소품 중심으로 다시 쓴다**: 그 소품을 `the subject of the picture`로 못 박고, 크기(`EXTREME CLOSE SHOT`)와 배치(`flat on the floor in front of her knees`)를 지정하고, 잘못 그려지던 대안을 부정문으로 막는다(`She is NOT at a window`). 인물이 여럿 뭉쳐 나오면 `Only these TWO people are clearly drawn; any others are far back, small and out of focus`를 덧붙인다.
- **★글자는 금지만으로 못 막는다 — 「그리되 읽히지 않게」를 포지티브로 준다** (2026-09-02 11편 2·3편 실측). `style.json` 네거티브에 `No text, no letters, no numbers`가 세 필드에 걸려 있는데도, 씬 `visual_desc`가 **문서·장부·방(榜)·간판을 요구하면 그 물건에 글자가 그려진다**(2편 씬44 옛 문서, 3편 씬29 담벼락 간판·씬39 장부·씬48 장부). 금지어는 「글자가 없는 화면」을 만들지만, 대본이 「글자가 있어야 할 물건」을 요구하면 포지티브가 이긴다. → 프리셋에 **`WRITTEN THINGS CARRY NO LEGIBLE MARKS: 종이와 장정은 그리되 글씨는 읽히지 않는 흐릿한 회색 붓자국으로만`**을 넣었다. 씬에서도 그 물건이 나오면 `★The pages carry NO readable writing`을 한 줄 덧붙인다.
- **★이름 없는 조연은 인종이 드리프트한다** (2026-09-02, 2편 씬39 포수가 서양인 얼굴로 나왔다). 주연은 턴어라운드 ref가 잡아 주지만 **조연·군중은 ref가 없어 텍스트 앵커가 유일한 방어선**이다. 프리셋에 `EVERY PERSON IN FRAME IS KOREAN, including unnamed background and supporting figures — 사냥꾼·아전·마을 사람·나그네·아이 / No Western faces, no brown or blond hair, no curly hair`를 넣었다.
- **★인물 색은 씬마다 드리프트할 수 있다 — 정색이 중요한 씬엔 lock 색을 씬에도 다시 적는다** (2026-09-02, 3편 씬22·37에서 옥례의 자주 저고리가 청록·흰색으로 바뀌었다). 턴어라운드 ref를 붙여도 100%는 아니다. 감정 정점이나 인물 식별이 걸린 씬에는 `she wears her own DEEP PLUM-PURPLE jeogori; her clothes must not change colour`처럼 **색을 한 번 더 못 박는다.**
- **★배경 시트는 `locations.json`의 `sheet` 필드를 채워야 씬 ref로 붙는다** (2026-09-03 13편 실측). `background.py`는 시트를 `assets/locations/{id}_sheet.png`에 굽지만 **필드를 되기록하지 않고**, `build.py`는 `locations[loc].get("sheet")`만 읽고 **경로 폴백이 없다.** 그래서 필드가 비면 배경 시트가 조용히 빠지고 **에러도 경고도 안 난다** — 씬은 그럴듯하게 나오므로 컨택트 시트로는 절대 못 잡는다. 12편이 이 상태로 나갔다. 점검: dry-run 로그의 `refs=N`이 **cast 수 + 1**인지 본다(장소가 있는 씬에서 cast 수와 같으면 시트가 안 붙은 것). **2026-09-04부터 `background.py`가 생성 직후 이 필드를 되기록한다** — 옛 프로젝트를 재생성 없이 쓸 때만 손으로 채우면 된다.
- **★★배경 시트를 붙이면 배경이 한 장으로 고정된다 — 씬마다 「장소 안 어디서 보는가」를 적어야 한다** (2026-09-04 사용자 지적 *"배경이 계속 고정되고 인물들만 위에 올라가는게 바뀌는 느낌"*). 위 `sheet` 필드를 채워 시트를 붙였더니 이번엔 반대 사고가 났다 — **장소가 지정된 117씬이 전부 시트와 같은 각도**로 나오고 인물만 그 위에 올라갔다. 배경 시트는 마당 전체를 한 각도에서 그린 그림 한 장이기 때문이다.
  - **원인은 지시문 부재였다.** `build.py`는 인물 시트에는 「얼굴·의상만 참조하고 포즈는 베끼지 말라」를 붙이면서 **배경 시트에는 아무 문장도 안 붙였다.** 2026-09-04에 대응 문장(`place`)을 추가했다 — 시트는 *이 장소가 무엇으로 되어 있는가*를 정의할 뿐 카메라 위치가 아니며, 씬마다 제 시점에서 본다.
  - **그러나 코드 문장만으로는 부족하다.** 씬 서술이 `MEDIUM SHOT, {돌복} waist-up at his work`처럼 **장소 안 위치를 안 말하면 모델은 시트로 되돌아간다**(실측: 3장 중 1장만 갈렸다). `visual_desc` 앞에 시점을 박아야 확실히 갈린다 — `Seen FROM INSIDE THE ROOM looking out through the open door` / `EXTREME CLOSE on the drying board` / `LOW ANGLE from the packed earth` / `Seen FROM OUTSIDE THE GATE looking IN` / `from a HIGH THREE-QUARTER ANGLE over her shoulder`. 13편 20씬에 넣었더니 전부 다른 화면이 됐다.
  - **부감은 과하면 인물을 물건 안에 넣는다** — `looking straight down at the vat`이 사람을 통 안에 그렸다. `HIGH THREE-QUARTER ANGLE` + `she KNEELS BESIDE it, NOT inside it`으로 낮춘다.
  - **점검**: `check_desc.py`가 **한 장소를 8씬 이상 쓰는데 그중 60% 이상에 시점 지시가 없으면 경고**한다(차단은 아니다 — 판단이 필요한 종류라). 그림을 뽑기 전에 본다.
  - ★12편은 `sheet`가 비어 있어 배경이 매 씬 새로 지어졌다 — 다양해 보이지만 **같은 집이 씬마다 다른 집**이었다. 시트를 붙이는 쪽이 옳고, 대신 시점을 써 준다.
- **★씬 프롬프트 2만 자에서는 style.json 네거티브가 진다 — 시대 소품은 씬 본문에 포지티브로 적는다** (2026-09-03 13편 실측). 현행 style.json은 씬 프롬프트를 **1.8~2.4만 자**로 만든다(12편도 같다). 이 길이에서는 끝쪽 `PERIOD PROP GUARD`의 `NO glass chimney … NO kerosene lamp`가 **밀려서 안 먹는다** — 밤 실내 씬에 서양식 유리 등피 램프가 그대로 나왔다.
  - **★★가드 문구 자체가 물건을 불러온다.** `EVERY lamp in this picture is a KOREAN OIL LAMP … no kerosene lamp`처럼 **금지어를 포함한 가드를 붙이면 원래 등불이 없던 씬에도 램프가 생긴다.** 실제로 3편 43씬은 대본·`visual_desc` 어디에도 등불이 없었는데 가드를 붙이자 램프가 세 개 나왔고, 그중 하나는 계속 유리였다. 세 번 고쳐 쓴 끝에 **낱말을 아예 빼는 방식**만 통했다.
  - **되는 방식**: 금지어(`lamp`·`lantern`)를 쓰지 않고 **물건을 서술로 지정**한다 — `a small NAKED FLAME in a SHALLOW OPEN BRASS SAUCER of oil set on the floor, nothing encloses the flame`. 밤 실내 씬은 광원을 안 적으면 모델이 스스로 램프를 만들어 넣으므로, **`night`+`interior`인 씬에는 광원을 반드시 명시한다.**
- **★장소 부정 문구(`NOT the …`)는 장소만 부정한다 — 앵커·소품을 부정하면 안 된다** (2026-09-03 13편 실측). 다른 장소 씬에 `location: null`을 주고 `NOT the smithy yard - no forge, no anvil, no wall of padlocks here.`처럼 접두사를 붙이는데, 여기에 **인물 앵커나 대본 소품이 섞이면 그것까지 지워진다.** 석주의 앵커가 '목에 건 자물쇠 꾸러미'인데 `no padlocks`가 같은 프롬프트에 있었고, 돌복 편은 `no hanging shoes`가 신을 들고 장에 가는 씬과 부딪쳤다. **점검**: 접두사의 금지 낱말이 그 편 `anchorProp` 9개와 같은 씬 본문에 겹치지 않는지 훑는다. 겹치면 건물·설비 쪽 낱말로 바꾼다(`no charcoal pile, no bellows` / `no tanning tub, no drying rack`).
- **★asset 검수는 contact sheet가 아니라 원본으로** — 썸네일에서는 얼굴 얼룩·각도 중복·화풍 편차가 안 보인다. `age spot`·`mole`을 lock에 적으면 큰 검은 반점이 한 패널에만 찍힌다(표식은 주근깨·홍조처럼 넓게 퍼지는 것만).
- **★시대는 프로젝트가 선언한다 — `{프로젝트}/era.txt`** (2026-08-29 신설). `style.json`은 **채널 공용**인데 시대는 편마다 다르다. 채널 기본값(`style["era"]`)에만 기대면 시대가 다른 편을 동시에 만들 때 조용히 섞인다. **실제 사고**: 다른 세션이 기본 era를 `modern1970s`로 바꾼 뒤 조선 편 16장이 **시멘트 블록 담·철 대문·슬레이트 지붕·자전거**가 있는 1970년대 마을로 생성됐다(프롬프트에 `Joseon`과 `1960s~1980s`가 **동시에** 들어간 모순 상태). 씬 자체는 멀쩡해 보여서 컨택트 시트로만 보면 놓치기 쉽다 — 인물이 한복·상투를 그대로 입고 있고 배경만 바뀌기 때문이다.
  - 우선순위 **`--era` > `{프로젝트}/era.txt` > `style["era"]`**. `build.py`·`turnaround.py`·`background.py` 셋 다 적용(`project_era()`).
  - **새 프로젝트를 만들면 `era.txt`부터 쓴다.** 없으면 채널 기본값을 따라가고, 그 값은 남이 언제든 바꾼다.
  - 점검: 생성 로그 첫 줄 `[era] joseon — …` 이 의도한 시대인지 본다. 사후 확인은 `grep -l "<다른 시대 문구>" {P}/scenes/_p*.txt`.
- **★장부 카드는 정확히 16:9로 굽는다** (2026-08-29). 예전 기본값 1344×768은 비율 1.750이라 16:9(1.7778)보다 **좁아서** CapCut 캔버스 좌우 양끝에 배경색이 닿지 않는 빈 띠가 남았다. 씬 이미지는 1376×768(1.792)로 도리어 약간 넓어 같은 문제가 없었기에 카드에서만 티가 났다. `insert_chapter_cards.py`의 `CARD_SIZE = (1920, 1080)`으로 고정했고, 글자·괘선 크기는 높이에 비례시켰다. 비율이 16:9가 아니면 경고를 찍는다.
- **★★화풍 = 소프트 로맨스 사극 웹툰 (2026-09-01 교체, 사용자 레퍼런스 기반)** — 종전 「굵은 선 + 하드 셀 + 고채도」에서 「얇고 또렷한 선 + 부드러운 그림자 + 전원 미형 얼굴 + 선명한 한복색」으로 바꿨다. 백업 `style.prev_20260901.json` · `style_anchor.prev_20260901.png`. 시안 이력 `projects/_styletest_look/` (A~R 16장 — 어느 방향이 왜 탈락했는지가 남아 있다).
  - **★얼굴 규칙이 바뀌었다** — 아래 「얼굴은 인물마다 다르게」의 6축 조합은 **평범한 얼굴을 전제로 한 옛 규칙**이다. 지금은 **전원에게 미형을 공통으로 걸고**(큰 쌍꺼풀 눈·또렷한 홍채·긴 속눈썹·작은 코·작은 입·갸름한 턱, 노인도 같은 화법으로 기품 있게) **구분은 얼굴형·눈매 기울기·눈썹 모양·머리 모양·색·앵커로 낸다.** 6축 중 「눈매(홑꺼풀)·코(뭉툭·낮고 넓은)·입(얇은)」은 미형과 정면으로 싸우므로 쓰지 않는다. `lead_face`(미모 등급)는 변별력을 잃어 비워 뒀다.
  - **★★lock이 style.json을 이기므로, 화풍을 바꾸면 인물 lock도 같이 다시 써야 한다** (2026-09-01 실측). style.json만 로맨스 웹툰으로 바꾸고 lock을 그대로 뒀더니 **인상이 하나도 안 바뀌었다** — lock의 `넓은 광대`·`각진 턱`·`가는 홑꺼풀 눈`·`굵은 일자 눈썹`·`낮고 넓은 코`가 미형 지시를 전부 눌렀다. 화풍 교체는 ①style.json ②인물 lock 전원 ③`style_anchor.png` **셋을 함께** 해야 한다. 하나라도 빠지면 옛 화풍이 그대로 나온다.
  - **★★`style_anchor.png`를 안 바꾸면 옛 화풍이 되돌아온다** — 앵커는 모든 턴어라운드에 ref로 붙는다. 옛 앵커(굵은 선·하드 셀 배경)를 그대로 두고 프리셋만 바꿨더니 시트가 전부 옛 결로 끌려갔다. **새 화풍으로 확정한 씬 한 장을 앵커로 삼는다**(현행 = 11편 1편 scene_23).
  - **★★「부드럽게」와 「흐릿하게」를 구분한다 — 프롬프트가 스스로 화면을 뿌옇게 만든다** (2026-09-01, 사용자 지적 *"왜캐 다 희끼무리하지? 하얗게 번진거같아"*). 로맨스 웹툰 결을 주문하면서 아래 넷을 쓰면 **화면 전체가 표백된다.** 넷 다 내가 넣었던 문장이고, 전부 걷어냈다.
    ① `광원에 넓고 부드러운 bloom 후광` · `warm bokeh circles` → **흰 번짐 그 자체.** 광원은 담긴다 — 등잔은 작은 불꽃, 등롱은 네모난 빛 덩어리로 그치고 화면으로 번지지 않는다.
    ② `얼굴 안쪽 선은 거의 없게` → 얼굴 구조가 뭉갠다. **얇되 또렷하게** — 쌍꺼풀선·속눈썹선·콧방울·입술선·턱선을 다 그린다. *"얇다는 것은 희미하다는 뜻이 아니다"* 를 프리셋에 문장으로 박아 뒀다.
    ③ `빛과 그림자 사이에 경계 없음`(전면) → 형태가 안 잡힌다. **그림자는 부드러운 가장자리를 갖되 분명히 자리 잡고**, 형태를 만들 만큼 깊어야 한다.
    ④ `피부가 화면에서 가장 밝은 것` → 하이라이트가 날아간다. 대신 **머리카락이 화면에서 가장 어두운 것**으로 잡아 대비를 세운다.
    네거티브에 `bloom·후광·번짐·안개·흰 막·과노출·선이 희미해지는 것` 금지를 함께 걸었다. 점검: 밤 씬이 **광원에서 떨어진 곳은 진짜로 어두운지**.
  - **★저채도 파스텔이 아니다** (2026-09-01 실측). 레퍼런스를 「low saturation / high value 파스텔」로 읽고 프리셋에 그대로 적었다가 **전체가 허옇게 표백됐다.** 실제 레퍼런스는 한복 색이 **선명하고**(분홍·진자주·청록·남색) 배경이 **따뜻한 갈색 어둠**이라 대비가 살아 있다. 「부드러운 화법」과 「낮은 채도」는 다른 축이다. 07편의 `FADED GREY` 사고와 같은 계열 — **색은 죽이지 않는다.**
  - **★홍채 색을 프리셋에 명시한다** (2026-09-01, 사용자 지적 *"눈동자가 너무 회색이라"*). `a big clearly coloured iris`처럼 **색 이름 없이 '또렷한 색'이라고만 쓰면 모델이 회색을 고른다.** `WARM DARK-BROWN iris — 초콜릿~호박 갈색, 테두리는 짙고 아래쪽이 밝게`로 못 박고, 네거티브에 `회색 눈·색 빠진 홍채·파랑/초록/보라 눈 금지`를 건다. **인물 lock이 아니라 채널 프리셋에 둔다** — 전원에게 걸려야 하고 새 프로젝트도 자동으로 물려받아야 하는 값이다.
  - **★비율은 negatives_turnaround로 잡는다** (2026-09-01, 곱단 실측). `broad through the shoulders and chest` 같은 **체구 서술이 로맨스 화법에서 비율을 깨뜨린다**(머리·어깨 비가 어긋난다). 큰 체구는 `comfortably PLUMP, a full softly rounded figure and natural adult proportions`처럼 **둥근 부피**로 쓰고, `NATURAL HUMAN PROPORTIONS — no oversized head, no exaggerated shoulder width`를 `negatives_turnaround`에 건다.
- **문화·시대 앵커는 STYLE 프리셋에 박는다** — ref 없는 씬(군중·빈 배경)은 텍스트 앵커가 유일한 방어선. 없으면 서양 판타지로 드리프트. `"Ghibli/anime"` 금지 → `"Korean webtoon/manhwa"`.
- **★시대(era) 자리표 (2026-08-29)** — 조선 앵커가 `preset`·`scene_preset`·`scene_preset_nocast`·`scene_negative`·`scene_negative_nocast` **다섯 군데에 그대로 박혀** 있던 것을 `{ERA_ANCHOR}`·`{ERA_PROPS}` 자리표로 모으고 `style.json`의 `eras`에서 골라 끼운다. **활성 시대 = `joseon`이고 그림 결과는 자리표 도입 전과 완전히 같다.** `turnaround.py`/`background.py`/`build.py`가 `resolve_era()`로 치환하며 `--era`를 받지만 **평소에는 쓰지 않는다.** `modern1970s`는 2026-08-29에 한 번 켰다가 되돌린 미사용 준비물이다 — 벤치 003·004를 근현대로 잘못 읽은 것이 원인이고, 그 두 영상은 조선 어휘 78~111회 / 근현대 어휘 0회의 완전한 조선 배경이었다(script-guide §10). **자리표를 지우면 시대 앵커가 통째로 빠져 서양 판타지로 드리프트한다** — 시대를 못 찾으면 스크립트가 즉시 멈춘다. 되돌리려면 `style.prev_20260829.json`.

## 워크플로우 (story-pd 스킬 상태머신)

```
SCRIPT → ASSET_GEN → STORYBOARD → SCENE_IMG → TTS → VEO_HOOK → RENDER → ATTACH_HOOK → INTRO → OUTRO → UPLOAD
(대본 후 대체로 자동 — 사람 게이트는 asset 검수(컨펌), TTS Vrew 낭독, CapCut 마무리 편집 셋.
 ★asset 검수 게이트 2026-07-19 재도입: ASSET_GEN 후 contact sheet를 `open`으로 창에 띄우고, 사용자 OK 전엔 STORYBOARD 진행 금지.
 ★TTS는 SCENE_IMG와 병렬 트랙: vrew export는 대본 확정 직후 실행, 낭독 도착 시 이미지 배치 중에도 ingest~SCENE_TIMING 선처리)
```

파일 존재 기반 상태 감지. 프로젝트 경로 `{P}` = `channels/{채널}/projects/{프로젝트}`.

**SCRIPT 서브 상태머신** (`{S}` = `{P}/_script`, ★주력 = **수면 옴니버스 — 독립 단편 2편 × 45분 = 90분 + 아웃트로 42분 = 약 2시간 12분, 대본 약 25,400자(편당 12,700자), 씬 111장** — playbook **v4.1**, 2026-09-03 사용자 지시로 3편×45분에서 개정. 속도 **284자/분** 승계(11편 실측 281.5)):
```
WATCHLIST(1회) → SCAN(소재 트렌드, URL 직접 지정 시 생략) → CONCEPT(★대화: 6편분 소재 일괄 + 편성표 확정 브리프) → OUTLINE(★대화: 편별 브리프 6개 + 배열표 + 레지스트리 초안)
→ DRAFT(1편 먼저 완성해 문체 기준 → 2편 병렬 가능 + bible=편별 인물 대장) → REVIEW(validate_script.py --style + 체크리스트) → script.txt+meta.txt
```
- 대본 공식(playbook) = story-pd `prompts/script-guide.md` — **매 편 벤치마킹 안 함**, 공식 버전 그대로 적용. 개정은 딥 벤치마킹(`benchmark-guide.md`) 제안 → 사용자 승인 시에만 (`playbook-history.md`에 기록).
- **★★편성 규격 = 2편 × 45분 + 아웃트로 42분 (2026-09-03 사용자 지시, guide §1 · playbook v4.1)**. 종전 3편×45분(05~13편)에서 편 수를 줄였다. 편 길이·속도·12비트 골격·계단 밀도는 그대로다.
  - 편당 **12,700자**(45분 × 284자/분) · 씬 **1편 66 + 2편 45 = 111장** · Vrew **4파트**(편당 a·b) · 장부 카드 2장 · 훅 2개.
  - **★★아웃트로는 30분이 아니라 42분이다 — 「2시간」은 목표가 아니라 하한선이다** (2026-09-03 검산). 사용자 지시는 「30분정도 붙여서 2시간짜리」였지만 **본편이 1:29:26뿐이라 30분을 붙이면 총 1:59:47로 미달**한다(자사 Vrew 실측 311자/분이면 1:52). 42분이면 2:11:47이고 최악의 속도에서도 2:04를 지킨다. 스캔에서 **2시간 이상 구간의 조회/일 배율이 1~2시간 구간의 두 배 이상**이라 그 선이 깨지면 배율이 통째로 바뀐다. **모자라면 분량을 늘리지 말고 아웃트로를 키운다**(대본 비용 0, 제목의 `[N시간]`은 내림 표기라 잃는 것 없음).
  - 아웃트로 자산 = **`channels/yadam/assets/intro,outro/sleep_noise_42min_quiet.mp3`** (폴더명에 쉼표. `attach_outro.py`는 `--outro` 필수). 30분·1h·2h·3h도 같은 폴더에 있다.
  - 길이 계산식 = **낭독 분 + 인트로 0:11 + 장부 카드 3초×편수 + 훅 Δ + 아웃트로** → 2시간 위로 **최소 5분 마진**.
  - **★편 수가 줄면 §7-1의 상한이 통째로 무력해진다** — 여덟 축이 전부 「같은 것 최대 2편」류라 2편 편성에서는 두 편이 같아도 통과한다. `check_sameness.py`도 임계가 `max(2, ceil(n×2/3))`이라 n=2면 위반·경고가 **둘 다 2**다. **2편 편성에서는 뼈대 축을 전부 「겹침 0」으로 읽는다**(guide §7·§7-1에 명시). 검사기 통과를 안심으로 받지 말 것.
  - §7-0 **자유 편은 2편 편성에서 선택 사항** — 두려면 2편(뒤 편)에만. 1편은 골격을 지킨다.
- **★v3.0은 포맷 전환이다** — 콜드 오픈·복선 장부·후렴·클리프행어·CTA·통쾌 감정축이 **전부 폐기**됐다(벤치 2편에서 0회). 이 채널은 이야기가 아니라 **수면 상황**을 판다: 목표는 "궁금해서 못 끄는 것"이 아니라 "편안해서 안 끄는 것". 감정축은 온기·보은 / 화해·용서. 근거: `channels/yadam/research/proposal_20260812.md`.
- `{S}/chapters/01.md`~(현행 `02.md`)는 **장이 아니라 편(篇)** — 편끼리 연속성 없음. 레지스트리 인물은 `story: N` 필드 + `s{N}_` id 접두사로 소속 편을 표시한다(편당 턴어라운드 2~4명 상한).
- **별도 인물 추출 단계 없음 (레지스트리 생애주기)** — characters/locations.json은 OUTLINE에서 서사 필드로 태어나고, DRAFT 중 동기화되고, ASSET_GEN 진입 시 시각 필드(영문 lock·negatives)를 완성한다.
- **★여섯 편이 한 편처럼 들리지 않게 한다** (2026-08-18 사용자 지시, guide §7-1). 소재 4축(주인공·결핍·촉발·해결)만 갈라 놓으면 부족하다 — **서술 방식이 같으면 같은 이야기로 들린다.** 03편 실측에서 여섯 편 전부 주인공이 과묵하고, 전부 소문에 대꾸하지 않고, 전부 손이 갈라졌다. 편마다 **주인공 기질**(과묵 최대 2편) · **⑥역풍 반응**(침묵 최대 2편) · **⑤수행 형태**(반복 노동 몽타주 최대 3편) · **⑧닿음 형태**를 다르게 배정하고 OUTLINE 브리프에 적어 둔다. REVIEW에서 **`check_sameness.py`**(위반 임계 = 편 수의 ⅔ 이상 공유 — 6편이면 4편, 3편이면 2편)와 **`pacing_check.py`**(설명 문단 연속 3개 이하)를 돌린다. 고칠 때는 표현을 동의어로 바꾸지 말고 **장치를 바꾼다** — 네 편이 침묵하면 그중 둘은 되받아 말하게 만든다.
- **★귀로 한 번 듣고 잡히게 쓴다** (2026-08-18 사용자 지시, guide §5-3·§5-5). 낱말이 다 쉬워도 **문장이 압축돼 있으면 못 알아듣는다** — 지시어로 시작하는 문장(그것이 며칠 뒤…), 뜻이 한 겹 숨은 관용구(셈이 아니라 사람을 씁니다), A가 아니라 B다, 절 셋짜리 문장이 범인이다. check_clarity.py로 잡는다. **시간은 앞으로만 흐른다** — 03편 1편이 셋째 해 → 둘째 해 → 첫해로 역행해 편 전체가 안 들렸다. 사건 블록을 끼워 넣으면 순서가 깨지므로 넣을 때마다 시간 표지를 다시 훑는다.
- **★반복으로 시간을 때우지 않는다 (playbook v3.3, 2026-08-27 — 08~10편 외부 리뷰 반영)**. 리뷰 지적: *"같은 종류의 노동이 반복되면 삭제 또는 통합한다"* · *"'계절이 바뀌었다' 대신 사건으로 시간을 넘긴다."* 기존 검사기 셋이 전부 사각지대였다 — `pacing_check`는 '인물이 없는 문단'을 보므로 노동 묘사는 통과하고, 9자 반복은 글자가 같아야 걸리고, `check_sameness`는 편과 편 사이만 본다. **`check_density.py`** 신설(guide §3-5): 같은 일감이 한자리에 몰린 **덩어리** 길이·개수, 인물 없는 **맨 시간 점프** 비율, 판이 안 바뀌는 **최장 구간**(분)을 잰다. ★**문단 수가 아니라 덩어리를 센다** — 문단 수로 세면 주인공의 생업이 편 전체에 흩어져 18편 전부 위반으로 찍힌다(09편 논밭일 54문단). 임계는 05~10편 18개 편 실측 분포에서 꼬리만 걸리게 잡았고(8/18 적발), 분포를 `lexicon/density.json`에 적어 뒀다. **사전은 코드 밖에 있고 채널별로 합쳐진다** — 못 잡는 일감이 생기면 규칙을 조이지 말고 사전을 늘린다. ★**감정 반복은 일부러 기계로 안 잰다** — 06편 1편이 14,327자에 감정 낱말 0개였다. 이 채널은 감정을 이름 붙이지 않고 행동으로 보이며, 그게 리뷰가 "유지하라"고 한 담담한 서술이다. 점수로 걸면 통과하려고 감정어를 집어넣게 된다. 사람이 본다(guide §2). ★**리뷰의 분 단위 비트표(12~15분 첫 반전·25분 최대 갈등·35분 감정 반전)는 넣지 않았다** — 반전·최대갈등은 §6 각성 금지이고, 시계표를 박으면 세 편이 같은 시계를 따라가 §7-0 자유 편이 성립하지 않는다. 취한 것은 "간격"뿐이다.
- **★규칙이 문장을 평평하게 만들지 않는지 본다 (playbook v3.1, 2026-08-20 — 근거 `research/proposal_20260820.md`)**. 다섯 호를 실측했더니 **상한만 있는 규칙이 전부 0쪽으로 수렴**했다: 40자 초과 문장 목표 "10% 이하"가 실제 **0.1~0.6%**(벤치 7.7·9.5%), 대사 "3.5% 이하"가 05편 **1.1%**, 어휘 다양도는 04편 0.580 → 05편 **0.495**로 떨어졌다. 셋을 바꿨다. ①**문체 목표를 밴드로** — 40자 초과 3~8% · 대사 2.5~4% · 의태어 ≥6/만자 · **반복 어휘 비율 ≤0.47**(v3.5에서 「어휘 다양도 ≥0.56」을 대체 — 3회 이상 나온 낱말이 차지하는 어절 비율. 벤치 4채널 0.31~0.35 vs 자사 05~10편 0.41~0.54로 겹치지 않는다. **6편 편성은 창이 짧아 읽지 않는다**) (`validate_script.py --style`이 잰다). ②**기계 자동 수정 중단** — `fix_breath --apply`(쉼표 삽입)와 `balance_endings --apply`(난수 어미 교체)는 검사 전용으로 내렸다. **호흡 상한도 20→26자**(자막은 `split_long_cues`가 13자로 다시 자르므로 대본 쉼표는 기준이 아니다). ③**편성마다 자유 편 하나**(guide §7-0) — 한 편은 9/12비트 골격을 깬다. 그리고 **규칙을 더할 땐 하나를 뺀다**(guide 「개정 규약」).
- **★편 순서를 바꾸면 편성표를 다시 짠다** (2026-08-18 실측, guide §4). 닫는 정형구는 편에 붙어 따라다니므로 **순서만 바꿔도 로테이션이 깨진다**(03편에서 1편↔5편을 바꿨더니 1·2편이 ③으로 겹쳤다). 정형구 6개 재검증 + §7 배열 규칙 전부 + 편별 분량(1편은 짧은 자리)을 다시 보고, **편별 프로젝트 폴더명·레지스트리 `sN_` id·`story` 필드를 새 번호로 함께 옮긴다**(안 옮기면 STORYBOARD가 다른 편 인물을 캐스팅한다).

**VEO_HOOK (SCENE_TIMING 후 — ★2026-08-27부터 기본 실행)** — v3.0에서 한 번 폐기했다가 되살렸다. 이유는 둘: 초반 이탈이 실제 문제로 남아 있고, pjn 엔진이 무료라 테이크를 얼마든지 뽑는다. **전제 = 1편 도입 첫 문장에 인물의 직접 대사 한 줄 + 그 화자가 씬1에 서 있을 것**(script-guide §3-0). `veo_hook.py`가 씬1 이미지+대사로 8초 립싱크 클립(오디오 포함)을 만들고, **렌더 규격 재인코딩 + 낭독 음량(`render.loudness_lufs`, 야담 −13.0)으로 자동 정규화**한다. **★`--speaker`로 화자를 지목한다** — 인물이 둘 이상인 씬에서 기본 문구('가운데 인물')는 모델에게 화자 선택을 맡기는 것과 같다. 함께 선 인물은 `--listener`로 입을 다물게 한다. **★렌더 storyboard에 주입하지 않는다**(`--inject` 기본 꺼짐) — 주입하면 capcut_export가 클립을 드래프트에 싣고 Windows CapCut이 죽는다(SKILL+ §9). 엔진: **`pjn`(무료·야담 기본 — 로컬 5090 서버 MiniMax H3 i2v, `.env`에 `PJN_API_KEY`, `image.veo.pjn.quality` 0.4~1.0; 최대 1376×768, 첫 프레임을 씬1 스틸에 픽셀 고정 → 컷 연결 자연스러움, H3 대사 태그 프롬프트는 `veo_hook_prompt_pjn.txt`로 분리)** / `gemini`(⚠️유료 API) / `flow`(웹세션, 데몬 필요). 실패해도 `{V}/veo_hook.json` 매니페스트를 남기고 RENDER 진행(도입부 스틸 폴백).

**ATTACH_HOOK (RENDER 다음, INTRO 앞 — 2026-08-27 신설)** — 훅 클립은 CapCut에 넣지 않고 내보낸 mp4의 **도입부를 갈아끼운다**. `attach_hook.py`가 자막에서 훅 대사 큐를 찾아 **선두 큐부터 그 대사 큐 끝까지**를 클립으로 교체한다 — 대사가 두 번 들리지 않고 영상이 사람의 말로 열린다(장부 카드는 그대로 앞에 남는다). 선두 12큐 안에서 대사를 못 찾으면 **자르지 않고 멈춘다**(영상 한복판을 자르는 사고 방지). 잘라낼 자리 뒤 첫 키프레임까지만 재인코딩하고 나머지는 스트림 복사 — 2시간 반을 다시 굽지 않는다. 클립 길이와 잘라낸 구간이 다르므로 이후가 Δ만큼 밀리고, **장부 카드 start/end와 meta.txt 챕터 시각을 함께 옮긴다**(그래야 뒤이어 도는 attach_intro의 챕터 재계산이 맞는다). 마커 `hook_attached.json`, 결과 `<완성본>_hook.mp4`.
- **★훅 클립에는 자막이 따로 안 붙는다 — 미리 구워야 한다** (`burn_hook_subs.py`, 2026-08-29 신설). 본편 자막은 CapCut이 입히는데 훅은 CapCut 밖이라, 그냥 두면 **영상 앞부분만 자막 없이 시작했다가 갑자기 생긴다.** 자막을 새로 쓰지 않고 **본편 `subtitle.srt`에서 그 대사 큐를 그대로 가져와** 굽는다(split_long_cues가 13자로 나눈 조각 그대로라 줄바꿈까지 본편과 같다). 스타일은 `burn_intro_subs.py`를 그대로 호출한다. 결과 `veo_hook_sceneNN_sub.mp4` — **`attach_hook`은 `_sub.mp4`가 있으면 그쪽을 쓴다.**
  - **★★말 시작 시각은 반드시 클립에서 잰다 — 프롬프트를 믿으면 안 된다** (2026-08-29 사용자 지적 *"veo hook에서 자막이랑 매칭이 안 되는 문제"*). 훅 클립은 **음성을 i2v 모델이 스스로 만든다.** 프롬프트에 `[0s-2s] 앰비언스 / [2s-8s] 발화`라고 적어도 **지키지 않는다** — 08편 세 클립 실측 발화 시작이 5.00s · 5.25s · 2.15s로 테이크마다 제각각이었다. 그래서 **자막 타이밍은 클립마다 재는 수밖에 없다.**
  - **★★재는 방법은 whisper 단어 타임스탬프다 — RMS 포락선도 앰비언스에 속는다** (2026-08-31 재지적 *"veo hook 자막이 매칭이 안돼"*). 훅 클립은 배경 소리를 모델이 같이 만드는데, **갯벌 편은 바람·물소리가 깔려** RMS 임계를 넘는다. 10편 실측 — 말은 2.16s에 시작하는데 RMS는 **0.45s**를 내놨다(1.71초 이르다). 8초짜리 클립이라 whisper는 몇 초면 끝나므로 정확도를 택한다. `burn_hook_subs.py`가 **whisper 우선, 실패 시 RMS 폴백**으로 돈다(`split_long_cues`의 whisper 금지는 두 시간짜리 낭독 얘기라 여기와 무관하다).
    10편 실측 오차: 씬02 RMS 4.25 / whisper 3.72 · 씬70 **RMS 0.45 / whisper 2.16** · 씬123 RMS 3.80 / whisper 3.20.
  - **★★모델이 프롬프트 속 한글 인물 이름을 소리 내어 읽는다** (2026-08-31, 사용자 지적 *"삼밭대 어른 어쩌고 하기 전에 뭐라 말하는데"*). 인물 `lock`이 **한글 이름으로 시작하고**(`갯골댁 — a 62-year-old Korean tidal-flat gatherer…`) 그것이 씬 `visual_desc`에 치환돼 프롬프트로 들어가면, MiniMax H3가 대사 앞에 **그 이름을 먼저 말한다.** whisper 받아쓰기로 확인 — 「복실도」·「개꿀덱」(갯골댁)·「봉출」이 본 대사 앞에 붙어 있었다. `--listener`로 지정한 인물 이름까지 읽는다. 그 결과 발화가 뒤로 밀려 자막이 4초 넘게 먼저 떴다.
    → `veo_hook.py`의 `strip_hangul_outside_dialogue()`가 **`<d>…</d>` 밖의 한글을 전부 지운다.** 한글이 필요한 곳은 대사 태그 안뿐이다.
    점검: 생성된 `veo_hook_prompt_pjn_sceneNN.txt`에서 대사 태그 밖에 한글이 없어야 한다. 의심되면 whisper로 받아써 본다 — 이름이 들리면 프롬프트가 오염된 것이다.
  - 재는 방법(폴백): 고정 dB 임계(`silencedetect`)는 쓰지 않고 RMS 포락선을 쓴다. i2v 클립은 배경 앰비언스가 깔려 있어 `-40dB` 같은 절대 임계를 **말이 아닌 소리도 넘는다** — 실제로 5.00s에 말하는 클립을 2.00s로 검출해 **자막이 3초 먼저 떴다.** 지금은 50ms RMS 포락선을 만들어 **하위 20%를 그 클립의 환경음 바닥**으로 잡고, 바닥의 4배 위로 0.2초 이상 유지되는 구간만 발화로 본다. 절대값이 아니라 **그 클립 자신의 분포**를 기준으로 하므로 테이크가 달라져도 맞는다.
  - 점검: `burn_hook_subs.py {P} --dry-run` 이 찍는 `발화 a~b s`가 클립을 눈으로 본 것과 맞는지. 어긋나면 자막이 목소리보다 먼저 뜨거나 늦게 뜬다.
  - **★훅을 다시 붙이려면 `insert_chapter_cards.py {P}` 부터 다시 돌린다** (2026-08-31 실측). `attach_hook`은 `{V}/subtitle.srt`를 **제자리에서 시프트한다.** 그 상태로 재실행하면 대사 큐를 못 찾아 「교체」가 아니라 **「삽입」**으로 잡히고, 그러면 훅과 본편에서 **같은 대사가 두 번 들린다.** 카드 단계는 멱등(`*.orig.*` 백업에서 다시 만든다)이라 한 번 돌리면 원상복구된다. dry-run 이 세 편 모두 「교체」로 나오는지 반드시 확인할 것.
- **★2026-08-28부터 훅은 편마다 하나씩 붙인다** (사용자 지시 *"훅 3편 다 붙일 거야 / 앞으로 프로젝트도"*, script-guide §3-0 개정). 2시간이 넘는 편성이라 챕터로 건너뛰어 듣는 사람이 있고 **편이 바뀌는 자리는 사실상 새 시작**이다 — §9-1 계단 밀도가 같은 이유로 2편 이후에도 초반 방어를 두는 것과 같은 논리다. 대본은 **편마다 첫 문장이 대사**이고, 편별 첫 씬(병합 보드의 씬 1·64·115 같은 자리)에 `hook_line`·`hook_speaker`가 적혀 있다. `veo_hook.py`는 이미 `--scene`으로 편별 클립을 뽑을 수 있다.
- **`attach_hook.py`는 편별로 붙는다** (09편 실측 2026-08-29 — 훅 3개 · 합계 시프트 +14.70s). 병합 보드에서 `hook_line`이 있는 씬을 편 시작으로 잡고, 그 시각부터 대사 큐를 찾아 컷마다 교체한다. 잘라낸 자리 앞뒤 키프레임만 재인코딩하고 나머지는 스트림 복사 — Δ는 컷마다 누적돼 장부 카드·챕터 시각에 함께 반영된다.
  - **★★옴니버스 훅 3종 세트 — 하나만 빠져도 조용히 1편 훅으로 뭉개진다** (2026-08-29 09편 실측, 세 군데를 한꺼번에 고쳤다).
    ① **`merge_omnibus.py`가 `hook_line`·`hook_speaker`를 병합 보드로 옮겨야 한다.** 안 옮기면 `burn_hook_subs`가 *"hook_line이 있는 씬이 없습니다"* 로 멈추고 `attach_hook`도 편 시작을 못 찾는다. 병합 보드를 다시 만들지 않고 채워 넣을 땐 편 폴더가 처음 나오는 씬에 편 하위 보드 씬1의 두 필드를 복사한다.
    ② **`veo_hook.py`의 파일명은 병합 보드 씬 id 로 짓는다.** 하위 보드 폴백이 `sc`를 편 하위 씬(항상 id=1)으로 갈아치우기 때문에, 종전에는 `--scene 57`·`--scene 108`을 줘도 세 편이 전부 `veo_hook_scene01`로 나와 **서로를 덮어썼다**(뒤 두 편이 1편 훅을 물려받았다). 확인: `_video/`에 `veo_hook_scene01/57/108`처럼 **편 수만큼 서로 다른 이름**이 있는지.
    ③ **시작 프레임도 씬 id 로 고른다.** `start_frame_path()`가 렌더 보드에서 무조건 `scenes[0]`을 집어 **2·3편 훅이 1편 씬1 그림으로 시작**했다. 확인: 매니페스트 `start_frame`이 그 편 폴더 경로인지.
  - **★★편 순서를 바꾸면 매니페스트의 `story` 필드도 옮겨야 한다** (2026-09-03 12편 실측). `attach_hook.py`는 파일명이 아니라 **매니페스트 `story`로 훅과 편을 짝짓는다**(`hooks.append({"story": m.get("story") …})`). 2편↔3편을 맞바꾼 뒤 그 필드를 그대로 뒀더니 2편 자리에 3편 대사가 걸렸고, 그 자리 자막과 안 맞으니 「교체」가 아니라 **「삽입」**으로 판정됐다 — 그대로 붙였으면 *같은 대사가 훅과 낭독에서 두 번* 들렸다. 고치는 법: 병합 보드의 `hook_line` 순서를 진실로 삼아 `_video/veo_hook*.json` 의 `story` 를 다시 매긴다. **dry-run 이 편마다 「교체」이고 대사가 그 편 것인지 확인하기 전에는 절대 붙이지 않는다.**
  - **★★`--scene` 은 렌더 보드 id 다 — 병합 보드 id 를 주면 엉뚱한 그림·엉뚱한 대사가 된다** (2026-08-31 10편 실측).
    `insert_chapter_cards` 가 편마다 카드를 한 장씩 끼우므로 **렌더 보드({V}/storyboard.json)는 병합 보드보다 id 가 밀린다.**
    10편: 2편 첫 씬이 병합 68 / 렌더 **70**, 3편이 병합 120 / 렌더 **123**. 병합 id 를 줬더니
    2편 훅이 1편 씬67 그림으로, 3편 훅이 2편 씬51 그림으로 생성됐다. **에러 없이** 그럴듯한 클립이 나온다.
    게다가 `veo_hook.py` 는 `--dialogue` 를 안 주면 **script.txt 의 첫 따옴표 대사**를 쓰므로 셋 다 1편 대사가 된다.
    → 편별 훅은 **`--scene <렌더 id> --story <편번호> --dialogue "<그 편 대사>"` 셋을 다 준다.**
    확인: 매니페스트의 `start_frame` 이 그 편 폴더의 `scene_01.png` 인지, `dialogue` 가 그 편 대사인지.
    그리고 `attach_hook --dry-run` 이 [1편]/[2편]/[3편]을 각각 다른 대사로 찍는지 — **붙이기 전에 반드시 dry-run.**
  - **★★가장 확실한 방법은 편 하위 프로젝트를 직접 대상으로 삼는 것이다** (2026-09-04 13편 실측). `--scene` 은 **병합 보드** id 로 서술·대사를 고르는데 `start_frame_path()` 는 같은 숫자를 **렌더 보드**에서 먼저 찾는다 — 장부 카드 때문에 두 보드의 번호가 어긋나므로, 병합 id 를 주면 2·3편 시작 프레임이 엉뚱한 씬이 되고 렌더 id 를 주면 서술·대사가 딴 씬 것이 된다. 대신 이렇게 부르면 둘 다 맞는다:
    `python3 scripts/render/veo_hook.py {P}/{편폴더} --config channels/{채널}/config/settings.json --video-subdir ../_video --scene 1 --story N --speaker "..." --out veo_hook_storyN.mp4 --manifest veo_hook_storyN.json`
    `start_frame_path()` 의 1순위가 `{project}/scenes/scene_01.png` 라 편 폴더를 주면 바로 맞고, 하위 보드 씬1에 `hook_line`·`hook_speaker` 가 있으니 대사도 맞는다.
    **★`--config` 를 반드시 명시한다** — 기본 경로가 `{P}/../../config/settings.json` 이라 편 폴더를 주면 한 칸 빗나가고, 그러면 **pjn(무료)이 아니라 gemini(유료) 프롬프트 형식으로 조용히 떨어진다.** 확인: 프롬프트 파일명이 `veo_hook_prompt_pjn_*.txt` 인지.
  - **★훅 자막은 「발화 시작」이 아니라 「대사 첫 낱말」에 맞춘다** (2026-09-04 13편 실측). `speech_window_whisper()` 가 whisper 의 **첫 낱말**을 발화 시작으로 잡았는데, 모델이 대사 앞에 다른 소리를 낸다 — 13편 2편은 대본의 「혼자 픽 웃고」를 그대로 연기해 웃음이 앞에 붙었고(4.36s→실제 대사 5.26s), 3편 첫 테이크는 머뭇거림(「음…」)이 붙었다. **프롬프트로 막으면 안 된다** — 금지어를 쓰면 그 소리를 부르고(램프 사고와 같은 계열), 대본에 있는 연기까지 막힌다. 2026-09-04부터 `speech_window_whisper(clip, expect)` 가 **대사의 첫 낱말과 맞는 낱말**을 찾아 거기서 시작한다. 인물 이름이 새어 나온 경우도 같은 방법으로 건너뛴다.
  - **★`insert_chapter_cards.py --restore` 는 `.orig` 백업을 지우지 않는다** (2026-09-04 실측). 그래서 **보드를 고친 뒤 재실행하면 묵은 보드가 되살아난다** — 13편에서 1편을 63장으로 늘린 뒤 카드를 다시 넣었더니 148씬짜리 옛 보드가 결과로 나왔다. 보드가 바뀌었으면 `--restore` 뒤에 `{V}/storyboard.orig.json`·`subtitle.orig.srt`·`audio.orig.mp3` 를 **지우고** 나서 `merge_omnibus → scene_timing → insert_chapter_cards` 순으로 돌린다. 확인: 카드 단계 로그의 「스토리보드 N씬」이 병합 보드 씬 수 + 편 수인지.
  - `burn_hook_subs.py` 는 병합 보드 id 로 파일명을 만들어 찾다가 못 찾는데, 2026-08-31에 **매니페스트 `dialogue` 로 클립을 짝짓도록** 고쳤다(이름이 달라도 찾는다).
  - **편을 갈아 끼웠으면 훅도 다시 뽑는다** — 09편 2편(「저녁 종」)은 08편에서 옮겨 온 이야기라 훅 클립의 `start_frame`이 **지금은 없는 08편 경로**를 가리키고 있었다. 매니페스트의 `start_frame`이 현재 프로젝트 안을 가리키지 않으면 그 클립은 버리고 다시 만든다(pjn은 무료다).

**★attach_hook·attach_intro·attach_outro 는 임시 폴더를 `C:` 에 쓴다 — 완성본이 10GB급이면 터진다 (2026-09-05 13편 실측)**
`attach_hook.py` 는 `tempfile.mkdtemp()` 로 작업 폴더를 만드는데 Windows 기본값이 `C:\Users\...\AppData\Local\Temp` 다. 13편 완성본은 **9.6GB** 였고 `C:` 여유가 2GB뿐이라 `No space left on device` 로 죽었다 — 그런데 **스크립트가 종료코드 0으로 끝나** 로그를 안 보면 성공한 줄 안다. 완성본은 외장(`D:`)에 있는데 임시물만 `C:` 로 가는 것이 함정이다.
→ 세 스크립트 모두 **임시 경로를 완성본과 같은 드라이브로 돌려서 실행한다**:
`TMPDIR="D:/_render_tmp" TEMP="D:/_render_tmp" TMP="D:/_render_tmp" python3 scripts/render/attach_hook.py …`
필요 공간은 대략 **완성본 크기의 2배**(조각 + 결과). 점검: 실행 전 `df -h /c /d`.


**★음량 기준 = 본편·인트로·훅을 모두 −12.8 LUFS로 맞춘다 (2026-09-05 13편 실측, 사용자 요청 「유튜브 평균보다 조금 높게」)**
- **YouTube 재생 정규화는 약 −14 LUFS이고 내리기만 한다** — −14보다 큰 영상은 그만큼 깎이고, **작은 영상은 올려 주지 않는다.** 그래서 파일을 아무리 키워도 체감 음량의 천장은 −14다. 본편을 더 키우는 것은 헤드룸만 잃는 헛수고다.
- 실측: 본편 10편 −12.91 / 11편 −12.80 / 12편 −12.82 / 13편 **−12.79** (TP −0.83). **이미 천장에 붙어 있다** — YouTube가 약 1.2dB 깎는다.
- **진짜 문제는 인트로였다 — −16.38 LUFS로 본편보다 3.6dB 낮다.** 영상이 조용히 시작했다가 갑자기 커진다. 훅도 편마다 −12.3~−14.5로 2.2dB까지 벌어진다. **고칠 곳은 본편이 아니라 인트로와 훅이다.**
- **★훅 목표는 −14가 아니라 「본편과 같은 값」이다** (2026-09-05 사용자 확인). YouTube가 −14로 내리기만 하므로 **영상 전체가 균일하면 어떤 값이든 재생 음량은 같다.** 훅만 −14로 맞추면 본편(−12.8)보다 1.2dB 작아져 **편이 시작될 때마다 작았다 커진다.** 값은 `settings.json` 의 `render.loudness_lufs`(야담 **−12.8**, 2026-09-05에 −13.0에서 변경)로 온다.
- **★`veo_hook.py` 의 종전 정규화는 두 가지로 빗나갔다 — 2026-09-05에 교체했다.** ①허용 폭이 **±1.0 LU**라 −14.05 클립이 「목표 −13.0 근처」로 판정돼 그냥 넘어갔고, 그래서 13편 훅 셋이 −12.31 / −14.05 / −14.52 로 **2.2dB 벌어진 채** 나갔다. ②`linear=true` + `target_offset` 재사용으로 착지가 어긋났다. 지금은 **측정 → 이득(`volume`) → 리미터(`alimiter=limit=-1dBTP`)** 이고 허용 폭은 **0.5 LU** 다(리미터가 피크를 깎아 0.2~0.35 LU 낮게 착지하므로, 0.3 으로 좁히면 재실행마다 또 굽는다). 실측 −13.15 / −12.84 / −13.08, 재실행 시 전부 생략(멱등).
- **★loudnorm 2패스에서 측정 패스의 `target_offset`을 재사용하면 안 된다** — 측정은 기본 목표(−24 LUFS) 기준이라 값이 어긋난다. 13편에서 −14.5 클립이 **−21.1로 떨어졌다.** 짧은 클립(인트로·훅)은 **측정 → 이득 조정(`volume`) → 리미터(`alimiter=limit=-1dBTP`)** 가 예측 가능하다. 결과: 인트로 −16.38→−12.77, 훅 −13.15/−12.84/−13.08.
- **아웃트로는 올리지 않는다** — 수면 노이즈는 일부러 조용하다(−35.7 LUFS, `_quiet`).
- 확인: 업로드 후 영상 우클릭 → **통계 정보**의 `Volume / Normalized`에 `content loudness N dB`로 깎인 양이 뜬다. 0dB에 가까울수록 천장에 붙은 것.
- ※ 이 환경에서는 **yt-dlp가 미디어만 403**이라 실제 YouTube 영상은 측정하지 못한다(자막·메타는 된다). 위 −14는 공개된 정규화 규격이고, 나머지는 자사 파일 실측이다.

**OUTRO (INTRO 다음, UPLOAD 직전 — 2026-08-21 신설 · ★2026-09-03부터 2편 편성에서는 42분이 기본)** — 이야기가 끝난 뒤에도 화면을 끄지 않는다. `attach_outro.py`가 `channels/{채널}/assets/intro,outro/sleep_noise_{길이}_quiet.mp3`에 (★폴더명이 `intro,outro` — 쉼표까지가 이름이다. `assets/outro/`에는 42분짜리 하나뿐이고 30분·1·2·3시간 자산은 전부 `intro,outro/`에 있다. `--outro`로 경로를 직접 준다) **완전 검정 화면**을 얹어(잠들 때 트는 자리라 그림을 깔면 빛이 남는다 — 어두운 밤 그림도 마찬가지) 본편 스펙으로 굽고 뒤에 concat `-c copy`(본편 재인코딩 없음). 정지 화면은 60초 조각 1개만 굽고 스트림 복사로 반복한다 — 통째로 인코딩하면 3시간에 1시간 넘게 걸린다. `_quiet`이 약 6dB 낮으니 기본은 quiet. 시작 3초 페이드인. 아웃트로는 뒤에 붙으므로 **챕터 시각을 밀지 않는다**. 마커 `outro_attached.json`. 결과 `<완성본>_intro_outro.mp4` — **업로드는 이 파일**.

**RENDER = CapCut export 전용 (자동 mp4 렌더 없음)**
★**`capcut_export.py`는 `{P}/_video`를 인자로 받는다**(`{P}`가 아니다 — `subtitle.srt`를 못 찾는다).
★**설정을 못 읽으면 조용히 코드 기본값으로 돈다** — 자막 `font_size` 10.0 → **5.0**, 켄번스 `pan_per_sec` 0.012 무시
  (씬 길이와 무관하게 한 번만 훑는 옛 동작). 드래프트를 다 만든 뒤에야 눈으로 알아채게 된다.
  2026-08-29에 **`--config` 없으면 상위로 올라가며 `config/settings.json`을 찾도록** 고쳤다(다른 스크립트와 같은 방식).
  옛 버전을 쓰거나 다른 채널에서 돌릴 때는 `--config channels/{채널}/config/settings.json`을 명시할 것.
  ★자막 **13자 분할과 `font_size` 10.0은 한 쌍이다**(settings 주석: 폰트 10 기준 한 줄 ~20칸이라 13으로 확정).
★**아웃트로 자산 경로는 `channels/{채널}/assets/intro,outro/`** — 폴더명에 쉼표가 있다. 문서·스크립트 예시의 `assets/outro/`는 없는 경로다.
`capcut_export.py`가 `{V}` 산출물로 CapCut 드래프트 생성(성공 시 `{P}/output/capcut_draft.json` 마커) → 사람이 CapCut에서 마무리 편집 → mp4 내보내기 → **ATTACH_HOOK → INTRO → OUTRO** → UPLOAD(upload.py가 output/의 mp4를 집는다).

**INTRO (RENDER 다음, UPLOAD 직전 — 2026-08-17 확정)** — 고정 인트로(`channels/{채널}/assets/intro/intro_ver3_bgm_sub.mp4` — ★파일명은 `attach_intro.py`의 `DEFAULT_INTRO` 상수에 박혀 있다. 승인본을 바꾸면 그 상수를 고친다. 옛 `intro.json`의 `approved_file`은 재빌드마다 덮어써져 날아가므로 더는 기준이 아니다)는 **CapCut에 넣지 않는다**(Windows CapCut이 타임라인 동영상 클립에 크래시하고, 편집 중 자막·씬 타이밍이 전부 밀린다). CapCut 내보내기 **뒤** `attach_intro.py`로 앞에 붙인다 — 인트로를 본편 스펙에 맞춰 정규화 → concat `-c copy`(본편 재인코딩 없음) → 챕터 시각 재계산(meta.txt). 완성본 폴더에 `intro_attached.json` 마커. ★01편은 이 단계를 빠뜨려 인트로 없이 완성됐다(사후 결합).
- **★`attach_intro`의 챕터 재계산은 장부 카드가 있어야 돈다** (2026-08-29 09편 실측). 렌더 storyboard에 `is_card` 씬이 없으면 *"스토리보드에 장부 카드가 없어 챕터 시각 계산을 건너뜁니다"* 를 찍고 **meta.txt를 손대지 않은 채 성공으로 끝난다.** 09편은 장부 카드를 CapCut 밖에서 처리해 카드가 없었고, 수록 편 시각이 인트로 길이(11.27s)만큼 통째로 어긋난 채 남을 뻔했다.
  - **챕터 시각은 편 시작 = 그 편 훅 클립의 첫 프레임**이다. 계산은 `원본 편 시작(자막 큐) + 앞선 훅들의 누적 Δ + 인트로 길이`. 아웃트로는 뒤에 붙으므로 밀지 않는다.
  - **★`attach_intro` 가 쓰는 챕터 시각은 1초 이르다 (2026-09-05 13편 실측)** — 장부 카드가 붙은 편성에서 계산값이 **카드 시작보다 1초 앞**을 가리켜, 그 시각으로 건너뛰면 **앞 편 마지막 자막**이 보인다(13편: 42:41→1편 「행복하게 살았습니다」, 실제 카드 시작은 42:42). 프레임으로 재서 meta.txt를 손으로 고친다. ★빠른 탐색(`-ss` 를 `-i` 앞에)은 키프레임으로 스냅돼 몇 초씩 어긋나므로 **`-ss (t-4) -i F -ss 4` 2단 탐색**으로 잰다.
  - **점검은 프레임으로 한다** — `ffmpeg -ss <시각> -i <완성본> -frames:v 1 out.png` 로 뽑아 그 편 훅 화면이 나오는지 본다. 09편은 낭독 예상치에서 밀어 온 종전 값이 **실제보다 최대 24초 뒤**였는데, 숫자만 보면 그럴듯해서 프레임을 보기 전엔 몰랐다.

**TTS 반수동(Vrew) 모드** — `settings.json tts.engine: "vrew"`. `ingest_vrew.py --export-script`(또는 `--chapters`)로 대본 내보내기 — **인라인 마크다운(`**강조**`)은 자동 제거된다**(2026-08-16, 별표가 Vrew 낭독·SRT에 그대로 들어가던 버그 수정)(★script.txt 확정 직후 즉시 — 낭독과 이미지 생성 병렬)(**1만자 한도 초과 시 `vrew_script_partNN.txt` 분할** — 파트별 낭독 후 `narration_NN.mp3/srt`로 저장, ingest가 샘플 정확도로 병합) → 사용자가 Vrew에서 음성+SRT 제작 → `{P}/vrew/`에 넣고 `ingest_vrew.py` 실행 → `audio.mp3`/`subtitle.srt`/`sentences.json` 생성 (ElevenLabs·alignment·무음압축 건너뜀) → **`split_long_cues.py --apply`로 자막 정형화** (①문장 경계 스냅: 문장 경계를 걸친 큐를 경계에서 분할 — 씬 경계=큐 경계 보장, ②20자 초과 분할; **타이밍은 음절 비례 분배 — whisper는 기본 끔**(2026-08-21 사용자 지시, 쓰려면 `--whisper`); --apply가 sentences.json에 문장별 실측 cue_range 기록; 반드시 scene_timing 전) → `scene_timing.py`부터 동일 (실측 cue_range가 있으면 정밀 모드, 없으면 비례 폴백).

## 폴더 구조

```
channels/{채널}/config/{style.json, settings.json, workflow.json, profile.md, watchlist.json}
channels/{채널}/research/           # scan_*.md 소재 리포트, topic_log.json 소재 대장(모티프 중복 회피), refs/ 딥 벤치마킹, proposal_*.md 공식 개정 제안
channels/{채널}/projects/{NN편}/           # ★영상 1건 = 폴더 1개 (01편, 02편 …)
  _script/{concept.md, outline.md, chapters/01~06.md}   # 영상 전체 대본
  script.txt  meta.txt  vrew/  _video/  output/
  {YYMMDD_HHMM_짧은제목}/                 # ★편(篇) 하나 = 하위 프로젝트 6개
    characters.json  locations.json  storyboard.json  assets/  scenes/
channels/{채널}/projects/{프로젝트}/       # (구) 단편·단일 서사 프로젝트
  _script/                          # concept.md, outline.md, chapters/, bible.md
  script.txt  meta.txt
  characters.json   locations.json
  assets/characters/<id>[_<variant>]_turnaround.png
  assets/locations/<id>_sheet.png
  storyboard.json
  scenes/scene_NN.png
  audio/  render/
scripts/
  research/scan_channels.py    # watchlist 채널 최근 영상 스캔 → 조회수/일 소재 리포트 (yt-dlp, 무료)
  research/collect_refs.py     # 영상 자막·댓글·메타 수집 (--benchmark=공식 개정용 / --project)
  script/validate_script.py    # 대본 제약 기계 검증 (분량·숫자·영어·26자 호흡·문어체 + --style 문체 밴드)
  script/check_meta.py         # ★meta.txt 규격 (업로드 전 — 설명문 확정 서식·챕터·해시태그)
  script/check_density.py      # ★사건 밀도 (같은 일감 덩어리·맨 점프·무전환 구간 — 사전은 lexicon/density.json)
  script/pacing_check.py       # 지루함 (설명 문단 연속·반복 어구)
  script/check_sameness.py     # 편끼리 같은 느낌인가 (상투구·어휘 겹침)
  image/generate_image.py      # 엔진 디스패처. flow=Nano Banana+2K 업스케일(무료 기본) / gemini=Nano Banana Pro API(유료)
  assets/turnaround.py         # characters.json → 턴어라운드
  assets/background.py         # locations.json → 배경 시트
  storyboard/build.py          # characters+locations+storyboard.json → 씬 이미지
  storyboard/check_desc.py     # ★씬 서술 감사 (구도·표정·캐스팅 — 그림 뽑기 전)
  render/capcut_export.py      # {V} 산출물 → CapCut 드래프트 (렌더는 CapCut에서 사람이 마무리)
  render/attach_intro.py       # CapCut 내보낸 mp4 앞에 고정 인트로 concat + 챕터 시각 재계산
  render/scene_timing.py       # 씬→자막 큐 타이밍 매핑 (렌더용 storyboard 생성)
  render/contact_sheet.py      # 검수용 그리드 몽타주 (PIL, scenes/·assets/ 공용)
  tts/  upload/                # autoworker에서 이식 (upload: OAuth 인증 + 비공개 업로드, 자족)
```

## 스키마

**characters.json** — id별 레지스트리. 같은 인물의 나이/상태 변화는 `variants`, 변신·환생은 새 id + 공유 `anchorProp`.
**locations.json** — 재등장 장소만 시트 ref.
**storyboard.json** — 씬: `{id, act, narration, cast[], location, visual_desc, image_prompt, image, status}`. cast는 `"id"` 또는 `"id:variant"`.

## 이미지 생성 규칙 (build.py)

- 프롬프트 = `visual_desc`({id}→variant별 trait-lock 치환) + 채널 `style.json`(STYLE+문화앵커+NEG)
- refs = cast별 turnaround + location.sheet → 최대 5장, cast 우선
- **★씬 예산 = 시간 기준 계단 밀도** (2026-08-22 신설 · **2026-09-02 사용자 개정**, guide §9-1). 러닝타임 × 계수가 아니라 **구간별 간격**으로 뽑는다.
  - **1편** — 0~7분 **20초** · 7~20분 **40초** · 20분~끝 **60초** → 45분 편이면 **66장**
  - **2편 이후** — **전 구간 60초** (종전 「0~5분 30초」는 폐기) → 45분 편이면 **45장**
  ★2026-09-02 개정 요지: 1편의 조밀 구간을 5분→**7분**으로 늘리고, 2편 이후는 초반 가중을 **없앴다**. 1편에 방어를 몰아 주고 나머지는 균일하게 간다. ★현행 2편×45분 편성이면 **111장**(1편 66 + 2편 45). 참고로 3편×45분은 약 163장이었다. 끊는 것은 글자수가 아니라 **낭독 실측 시각**이다 — `cut_by_time.py --plan`(낭독 전에는 `--estimate {cpm}`). 전 씬 Ken Burns가 전제(CapCut 마무리). 밀도-조회수는 무관하다는 실측(자사 23편 상관 +0.09)이 저밀도의 근거.
  - **★낭독이 도착하면 밀도를 다시 잰다** (2026-09-02 11편 실측). 추정 시각으로 끊어 둔 보드는 실측과 어긋난다. 11편은 잠정 284자/분 → 실측 **281.5**로 거의 맞았는데도, **규칙 자체가 바뀌면** 1편이 세 구간 모두 미달로 나왔다(0~7분 24.7초 · 7~20분 43.3초 · 20분~ 63.2초).
  - **★보드를 다시 끊지 말고 몇 장만 쪼갠다.** 재단(`cut_by_time` 재실행)은 경계가 전부 바뀌어 **작성해 둔 `visual_desc`가 통째로 무효**가 된다. 대신 **구간별로 가장 긴 씬을 골라 문장 경계에서 쪼갠다** — 쪼갠 자리는 반드시 *내용이 바뀌는 지점*이어야 하고(누렁이가 뛰어드는 순간, 국자에서 가을 혼사로 넘어가는 자리), 앞쪽도 desc를 새로 써서 **두 쪽 다 재생성**한다. 11편 1편은 이 방법으로 7장만 추가해 57→64가 됐다.
  - 쪼개면 뒤 씬 번호가 밀리므로 **`scene_NN.png`·`_pNN.txt`를 새 번호로 옮긴다**(번호 큰 것부터, 충돌 방지). 그리고 **앞쪽 desc에 인물을 새로 등장시켰으면 `cast`에도 넣어야 한다** — 11편에서 실제로 빠뜨려 검증기가 잡았다.
- 모델(flow 기본): Nano Banana(NARWHAL) + 2K 업스케일, 동시성 ~5 — banana-pro와 일관성 동급이면서 일일 쿼터 ~10배(A/B 실측 2026-07). `settings.json image.flow.model`로 변경

## 이미지 엔진 (flow=무료 / gemini=유료)

`settings.json image.engine`로 분기(**미설정 시 `flow`**; ★yadam 채널은 2026-07-25부터 `gemini` 명시 — flow 일일 한도 회피, 씬 예산 ~160장으로 비용 상쇄). `generate_image.py`가 이 값을 읽어 자동 라우팅 — 호출부(turnaround/background/build) 무관.
- **flow**(무료, 기본): labs.google Flow 웹세션(버너 구글 로그인)으로 Nano Banana(NARWHAL) 생성 + `upsampleImage` 2K 업스케일(실패 시 원본 폴백). **상주 데몬 + Chrome Flow 탭 로그인 필요**(reCAPTCHA 브리지, 반수동). 셋업: `docs/flow-image.md`.
- **gemini**(유료, 명시 opt-in 전용): `settings.json image.engine="gemini"`로 **직접 지정 + `.env`에 `GEMINI_API_KEY` 등록**했을 때만 API 호출. **자동 폴백 없음** — flow가 깨져도 gemini로 몰래 넘어가지 않고 에러를 낸다(실수 과금 방지). 키 없이 gemini 지정 시 안내 메시지 후 종료.

## 환경

- API 키: 루트 `.env`의 `GEMINI_API_KEY` (gemini 엔진 전용).
- `generate_image.py`는 상위 폴더로 올라가며 `.env`(키)와 `config/settings.json`·`config/style.json`(엔진·비율)을 찾는다.
- flow 엔진: `python3 scripts/image/flow_token_server.py` 상주 + Chrome 확장(`scripts/image/flow_extension/`) Connect. 토큰은 `~/.flow-proxy/token.json`.
